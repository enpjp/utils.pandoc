This package forces the use of a specific version of pandoc when compiling PDFs in RStudio using the knit button:

To use add the following to your YAML:

      knit: utils.pandoc::knit_with_pandoc


      
